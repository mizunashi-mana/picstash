# タスク: ディレクトリ構成ドキュメントの分割

## 目的・ゴール

`.ai-agent/structure.md` のディレクトリ構成図が巨大（約340行）になっているため、以下の構成に分割する：

1. **全体概要図** (`structure.md`) - プロジェクト全体の大まかな構成のみ
2. **パッケージ別詳細図** (`structures/` ディレクトリ) - 各パッケージの詳細構成

## 実装方針

### structure.md 内の構成

1. **全体概要図** - プロジェクトルートから packages/ までの大まかな構成
2. **パッケージ別詳細図** - 各パッケージごとにセクションを分けて詳細を記載
   - web-client
   - server
   - core
   - api
   - desktop-app
   - eslint-config
3. **主要ディレクトリの説明** - 既存の説明セクションを維持

### 全体概要図の内容

- `.ai-agent/` の構成
- `packages/` 配下のパッケージ一覧（1行説明のみ）
- `storage/`, `scripts/`, ルートファイル群

### 各パッケージ詳細図の内容

- そのパッケージ内の詳細なディレクトリ構成
- 必要に応じて簡単な説明

## 完了条件

- [ ] 全体概要図が簡潔にまとまっている
- [ ] 各パッケージの詳細図がセクション分けされている
- [ ] 元の情報がすべて保持されている
- [ ] 読みやすい構成になっている

## 作業ログ

### 2026-02-08

- structure.md を再構成
  - 全体概要図（30行程度）を冒頭に配置
  - 各パッケージごとにセクションを分割（図 + 説明をセット）
  - テスト構成は各パッケージのセクション内に統合
- 変更前: 340行（1つの巨大な図 + 説明セクション）
- 変更後: 374行（全体概要 + 6パッケージの詳細セクション）
  - 行数は増えたが、セクションごとに読みやすくなった
