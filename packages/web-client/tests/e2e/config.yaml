# Picstash Server Configuration for E2E Tests

server:
  port: 4000
  host: "0.0.0.0"

database:
  path: "./prisma/data/picstash.db"

storage:
  path: "./data/storage"

logging:
  level: "info"
  format: "pretty"
  file:
    enabled: true
    path: "./data/server.log"
    rotation:
      enabled: false
